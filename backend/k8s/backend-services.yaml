apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: task-manager
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: task-manager
  labels:
    app: task-service
spec:
  selector:
    app: task-service
  ports:
  - port: 80
    targetPort: 3002
    protocol: TCP
    name: http
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: task-manager
  labels:
    app: media-service
spec:
  selector:
    app: media-service
  ports:
  - port: 80
    targetPort: 3003
    protocol: TCP
    name: http
  type: ClusterIP
